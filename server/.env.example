# Outlook/Hotmail SMTP configuration
# If you have 2FA enabled, generate an App Password:
# https://account.live.com/proofs/manage/additional
EMAIL_USER=your-email@outlook.com
EMAIL_PASSWORD=your-app-password-or-regular-password

# Business email (where orders go, defaults to EMAIL_USER if not set)
ORDER_EMAIL=zamowienia@galaretkarnia.pl

# Frontend URL (for CORS - currently using wildcard '*' for testing)
FRONTEND_URL=https://galaretkarnia-pl.vercel.app

# Environment
NODE_ENV=production
PORT=3001

# Optional: Send confirmation to customer?
CUSTOMER_EMAIL_FIELD=false
